package com.spring.sheepshop.config;

import org.springframework.context.annotation.Configuration;
import org.springframework.scheduling.annotation.EnableScheduling;

@Configuration
@EnableScheduling
public class SchedulingConfig {
    // Đây có thể là nơi để đặt các cấu hình liên quan đến lịch trình, nhưng trong trường hợp này, chỉ cần annotation là đủ.
}
